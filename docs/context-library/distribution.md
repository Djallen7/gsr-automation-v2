# Distribution

**Load when:** delivery targets, platforms, vendors, "where does the finished episode go", file handoff.
**Authoritative source (truth):** `docs/_handoff/GSR-WORKFLOW-CANON.md` §11 (Established Distribution Stack & Vendor Registry), the live `distributions.platform` enum (latest migration altering `distributions_platform_check`), `config/production.json`. Web/memory only SUPPLEMENT these.

## Entries
### standing — Established stack must not be omitted  [status: active]
Decision: any answer about distribution must reconcile against canon §11 + the enum + config first. Known established/deferred items include StreamHoster, Signiant/RLN, Dropbox-as-distribution, and deferred GodTube/OTA/TBN/CTN targets — if an answer omits what these sources contain, it's wrong; fix before sending.
Source: `CLAUDE.md` Established-facts rule
(Open this category's authoritative docs for the full current registry; add dated changes here as Daniel makes them.)
