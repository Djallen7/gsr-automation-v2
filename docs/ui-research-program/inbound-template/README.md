# Inbound template

Drop the downloaded UI template zip (or its unzipped files) here.

Once it is uploaded and pushed to the branch `claude/pipeline-ui-design-strategy-Nzivm`, tell me and I will:
1. Pull the branch and unzip it (then delete the zip so it does not bloat the repo).
2. Read the markup, styles, and components.
3. Use it per your instruction: match its look for the prototypes, lift specific components, or rebuild one concept (Mission Control / Ambient / Broadcast) inside its shell.

When you upload, note:
- What it is (plain HTML/CSS, React or Next + Tailwind, a shadcn theme, etc.).
- What you want done with it.
- Its license, if known.
